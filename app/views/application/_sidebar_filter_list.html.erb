<ol class="c-sidebar_container__list">
  <% filters.each do |filter| %>
    <%= render :partial =>
      "sidebar_#{filter.class.name.downcase}",
      locals: {
        filter_item: filter
      }
    %>
  <% end %>
</ol>
