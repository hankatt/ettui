<li>
	<%= check_box_tag "source_ids[]", source.id, false, :id => "source_ids_#{source.id}" %>
	<div class="source-favicon" style="background-image: url('<%= source.favicon %>');"></div>
	<button class="sidebar-list-button"><%= source.hostname.truncate(30) %></button>
	<div class="filter-count"><p><%= count_source_occurrences(source.id) %></p></div>
</li>