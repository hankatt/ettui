//Rebuild grid with new elements
var $new_items = "<%= escape_javascript(render(@quotes)) %>";
var $container = $(".quotes-container");

$container.html('');

//$container.masonry('destroy');

$container.prepend($new_items);

search_query = $("#search-filter .search").val();
quotes = $(".quote .quote-content p");
quotes.unhighlight().highlight(search_query);

$(document).trigger('DOMChange');

//ini();

// initialize
//$container.masonry();