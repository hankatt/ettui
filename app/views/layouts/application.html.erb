<!DOCTYPE html>
<html>
<head>
  <title>Well</title>
  <link rel="shortcut icon" href="/assets/favicon.ico" />
  <link href='http://fonts.googleapis.com/css?family=Muli:300,400' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Josefin+Slab:400,700' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <script type="text/javascript">

  var _gaq = _gaq || [];
  _gaq.push(['_setAccount', 'UA-31860470-1']);
  _gaq.push(['_trackPageview']);

  (function() {
    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
  })();
</script>
</head>
<body>

<header>
	<%= image_tag "well-w.png", :height => "28" %>
	<a href="<%= root_path %>">
		<h1 style="display: inline;">Well said</h1>
	</a>
  <div id="login-container">
    <% if current_user %>
      <p>Hey there, </p>
      <%= link_to (@user.email || @user.name), quotes_path, :class => "white mleft-s" %>
      <%= link_to "Logout", logout_path, :class => "mleft-s red" %>
    <% else %>
      <p>Been here before?</p>
      <%= link_to "Login here ", login_path, :class => "mleft-s white" %>
      <p class="mleft-s">or </p>
      <%= link_to "through Twitter", "/auth/twitter", :class => "mleft-s" %>
    <% end %>

  	<!--
  	<% flash.each do |name, msg| %>
  		<%= content_tag :div, msg, :id => "flash_#{name}" %>
  	<% end %>
  	!-->
  </div>
</header>
<div class="divider"></div>
<div class="container">
	<%= yield %>
</div>

</body>
</html>
