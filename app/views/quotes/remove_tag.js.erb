// Fade out and remove the removed tag
deletedTag = $(".quote-tags.q-<%= @quote.id %> .t-<%= @tag_id %>");
console.log(deletedTag);
$(deletedTag).remove();

if(<%= @tag_destroyed %>) {
	popupTag = $(".popup ul .t-<%= @tag_id %>");
	console.log(popupTag);
	$(popupTag).remove();
}