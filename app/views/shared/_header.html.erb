<header>
    	<a href="/" id="logo-container">
	        <div></div>
		</a>
		<% if !current_user %>
		    <div class="inline link">
		        <p>Have an account?</p><%= link_to " Sign in", login_path %>
		    </div>
		<% else %>
			<div class="inline link">
				<p>Welcome back, </p><%= link_to current_user.name || current_user.email, boards_path %>
			</div>
		<% end %>
</header>