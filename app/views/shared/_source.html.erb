<li class="sidebar-list-item <%= @search.source_classes(source) %>" >
	<h2 class=""><%= source.id %></h2>
	<div class="source-favicon" style="background-image: url('<%= source.favicon %>');"></div>
	<%= link_to source.hostname.truncate(30), filter_board_quotes_path(@board, @search.update_params_for(source)), class: "sidebar-list-button" %>
	<div class="filter-count"><p><%= count_source_occurrences(source.id) %></p></div>
</li>